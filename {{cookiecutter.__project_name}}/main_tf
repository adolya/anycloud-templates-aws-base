terraform {
  backend "s3" {
    region         = "{{ cookiecutter.AWS_REGION }}"
    bucket         = "terraform-state-{{cookiecutter.__project_name}}-base"
    key            = "base.tfstate"
    dynamodb_table = "terraform-{{cookiecutter.__project_name}}-locks"
    encrypt        = true
  }
}
